# Arquivos compilados do Maven
target/

# Configurações locais com dados sensíveis
# application.properties agora e versionado (nao tem mais segredo nenhum,
# so ${VAR} sem valor real) — so o arquivo local com os segredos e ignorado
**/application-local.properties

# IDEs
.vscode/
.idea/
*.iml

# Sistema operacional
.DS_Store
Thumbs.db

# Logs
*.log

# Claude Code
.claude/
